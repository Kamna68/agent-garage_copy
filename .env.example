# AgentGarage Environment Configuration
# Copy this file to .env and customize the values

# ==============================================
# PostgreSQL Database Configuration
# ==============================================
# Used by n8n for storing workflows, credentials, and execution history
POSTGRES_USER=root
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=n8n

# ==============================================
# n8n Security Keys
# ==============================================
# IMPORTANT: Generate unique keys for production!
# Use: openssl rand -base64 32

# Encryption key for credentials and sensitive data
N8N_ENCRYPTION_KEY=generate_a_random_32_character_key_here

# JWT secret for user session management
N8N_USER_MANAGEMENT_JWT_SECRET=generate_another_random_key_here

# Binary data storage mode (filesystem or default)
N8N_DEFAULT_BINARY_DATA_MODE=filesystem

# ==============================================
# Jira Integration (Optional)
# ==============================================
# Only needed if you plan to use the Jira-related workflows
# Leave as-is if not using Jira integration

# Your Jira personal access token
# Generate at: http://localhost:8080/secure/ViewProfile.jspa → Personal Access Tokens
JIRA_PERSONAL_TOKEN=your_personal_access_token

# Your Jira project key (e.g., PROJ, DEV, TEST)
JIRA_PROJECT=your_project_key

# Your Jira username
JIRA_USERNAME=your_jira_username

# ==============================================
# Open WebUI Configuration
# ==============================================
# URL for Open WebUI service (keep as-is for Docker setup)
WEBUI_URL=http://openwebui:8080

# ==============================================
# Advanced Configuration (Optional)
# ==============================================

# Uncomment and customize these if needed:

# n8n timezone (default: UTC)
# GENERIC_TIMEZONE=America/New_York

# n8n log level (options: info, debug, warn, error)
# N8N_LOG_LEVEL=info

# Disable n8n diagnostics and telemetry
# N8N_DIAGNOSTICS_ENABLED=false
# N8N_PERSONALIZATION_ENABLED=false

# Ollama host (keep as-is unless running Ollama externally)
# OLLAMA_HOST=ollama:11434

# Webhook URL (keep as-is for Docker setup)
# WEBHOOK_URL=http://n8n:5678

# ==============================================
# Security Notes
# ==============================================
# 
# 1. NEVER commit the actual .env file to version control
# 2. Generate strong random keys for production deployments
# 3. Use different passwords for each environment (dev/staging/prod)
# 4. Rotate credentials regularly
# 5. Keep this file secure - it contains sensitive information
#
# To generate a secure random key:
#   openssl rand -base64 32
#
# Or in Python:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
#
# ==============================================

# ==============================================
# Quick Start
# ==============================================
# 
# 1. Copy this file:
#    cp .env.example .env
# 
# 2. Generate secure keys and update:
#    - N8N_ENCRYPTION_KEY
#    - N8N_USER_MANAGEMENT_JWT_SECRET
#    - POSTGRES_PASSWORD
# 
# 3. (Optional) Configure Jira if using those workflows
# 
# 4. Start the environment:
#    docker compose --profile cpu up
#
# ==============================================
